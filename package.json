{
  "name": "plugin-add-category-on-wrap-up",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "flex-plugin pre-script-check",
    "start": "twilio flex:plugins:start",
    "deploy": "twilio flex:plugins:deploy --major --changelog 'Changes1' --description 'Description1' --profile akFlex2",
    "release": "twilio flex:plugins:release --plugin $npm_package_name@$npm_package_version --name 'Release from nmp script' --description 'The description of this Flex Plugin Configuration' --profile akFlex2"
  },
  "dependencies": {
    "flex-plugin-scripts": "4.7.5-beta.0",
    "react": "16.5.2",
    "react-dom": "16.5.2"
  },
  "devDependencies": {
    "@twilio/flex-ui": "^1.26.3",
    "react-test-renderer": "16.5.2",
    "typescript": "^3"
  }
}